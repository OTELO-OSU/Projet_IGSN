export const sampleLandingPage = (igsn: string, frontendUrl: string): string =>
  `${frontendUrl}samples/${igsn}`;

export const tombstonePage = (frontendUrl: string): string =>
  `${frontendUrl}tombstone`;
