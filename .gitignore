# Dependencies
**/node_modules

# Build files
**/*.tsbuildinfo
**/dist
**/dist-ssr
**/.nitro
**/.tanstack
**/.output
**/.vinxi
**/__unconfig*
**/__screenshots__
**/.vitest-attachments

# Uploaded sample attachment blobs (dev/test/e2e debug output, ADR 0017).
# The api writes them under its cwd, which is the repo root for `pnpm test`.
packages/api/attachments
/attachments

# Environment files
**/*.local
**/.env
infra/preprod/docker-compose.env

# Cloudflare Origin CA cert + key (host secrets, copied by the operator)
infra/preprod/certs/

# MacOS files
**/.DS_Store

# Claude
.claude/settings.local.json

# Playwright
**/test-results
**/playwright-report
**/.playwright
**/.vitest-attachments

# Dev Container
.devcontainer/.mnt-*/*
!.devcontainer/.mnt-*/.gitkeep
.pnpm-store
.ms-playwright
## Local development container configuration files
.devcontainer.json
.devcontainer-lock.json

# Legacy IGSN dump, imported via `make db-import-legacy` (never committed)
bdd-igsn.sql
# Legacy import output, tee'd by `make db-import-legacy` (skipped rows, summary)
import-legacy.log
.playwright-mcp
