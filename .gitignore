**/node_modules

**/*.tsbuildinfo
**/dist
**/dist-ssr
**/.nitro
**/.tanstack
**/.output
**/.vinxi
**/__unconfig*
**/__screenshots__
**/.vitest-attachments

# Uploaded sample attachment blobs (dev/test/e2e debug output, ADR 0017).
# The api writes them under its cwd, which is the repo root for `pnpm test`.
packages/api/attachments
/attachments

**/*.local
**/.env
infra/preprod/docker-compose.env

# Cloudflare Origin CA cert + key (host secrets, copied by the operator)
infra/preprod/certs/

**/.DS_Store

.claude/settings.local.json

**/test-results
**/playwright-report
**/.playwright
**/.vitest-attachments

.devcontainer/.mnt-*/*
!.devcontainer/.mnt-*/.gitkeep
.pnpm-store
.ms-playwright
.devcontainer.json
.devcontainer-lock.json

# Legacy IGSN dump, imported via `make db-import-legacy` (never committed)
bdd-igsn.sql
import-legacy.log
.playwright-mcp
