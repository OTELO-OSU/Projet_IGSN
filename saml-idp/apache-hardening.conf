# Strip the Apache version banner and the ServerSignature footer from error
# pages and directory listings, so the simplesamlphp IdP does not leak
# "Apache/2.4.57 (Debian) Server at ... Port 80" at the public edge.
# Loaded via conf-enabled; the zzz- mount name sorts after Debian's security.conf.
ServerTokens Prod
ServerSignature Off
